import SignupPageView from "@/views/signup";

export default function SignupPage() {
  return <SignupPageView />;
}

// Register (fullName, email, password, confirmPassword)
// Send an email to verify -> If sigin with google then automatically verify
// After verification, show a screen to add website
// Add tracking code option
// Verify installation and navigate them to dashboard.
